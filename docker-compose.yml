version: "3.8"

services:
  walkieclaw-bridge:
    build: .
    ports:
      - "8080:8080"
      - "12345:12345/udp"
    env_file: .env
    restart: unless-stopped
    volumes:
      - bridge-audio:/tmp/walkieclaw-audio

volumes:
  bridge-audio:
